# Fullstack 2016 JSON Schema Demo

## Usage
1. Download the [Scala Build Tool](http://www.scala-sbt.org/download.html)
2. Navigate to the project directory and execute `sbt`
3. Download any NPM dependencies by executing the `getNpmDeps` command
4. Finally, start the application by entering `run`
